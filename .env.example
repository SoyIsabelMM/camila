# Base path for deployment. Leave empty or unset for root (e.g. my.domain.com/).
# Set to /camila (or your subpath) to deploy under a subpath (e.g. my.domain.com/camila/).
# NEXT_PUBLIC_BASE_PATH=/camila
